apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "taskapi.fullname" . }}-config
  labels:
    {{- include "taskapi.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
